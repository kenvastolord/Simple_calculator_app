{
  "homepage": "https://kenvastolord.github.io/Simple_calculator_app/",
  "version": "0.0.1",
  "description": "A simple calculator built with HTML, CSS, and JavaScript",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "tailwindcss": "^3.4.12"
  },
  "scripts": {
    "build": "npx tailwindcss -i ./public/styles.css -o ./src/styles.css --minify",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ."
  }
}
